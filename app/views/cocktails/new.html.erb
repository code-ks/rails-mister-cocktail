<%= simple_form_for(@cocktail) do |f| %>
  <% if @cocktail.errors.any? %>
    <%= @cocktail.errors %>
  <% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>

<%= link_to 'Back', root_path %>

